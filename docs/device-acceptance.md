# 完整真机验收清单

日期：2026-09-07。依据：requirements.md AC-01—AC-11、handoff.md 已确认计划及当前源码。

本清单已准备完成；以下手机操作全部待执行。Android 首页显示有此前用户确认，其余项目逐项记录结果。电脑测试通过与手机通过分别登记。

## 1. 开始前记录

| 字段 | 本次记录 |
|---|---|
| 测试日期、执行人 | 待填 |
| 手机型号、Android 版本、微信版本 | 待填 |
| 开发工具、基础库版本 | 实测前填写；历史记录为 2.02.2608060、3.17.2 |
| 工程及版本 | 当前目录，package.json 0.1.0；记录本次编译时间与代码版本或文件哈希 |
| AppID、运行方式 | 配置为 wx32215e8bca6859ed；交接记录为测试号；勾选真机调试／预览／正式账号体验版 |
| 材料数量、总字节、最大文件、图片尺寸 | 待填 |
| 开始时可用存储、生成耗时、输出大小 | 待填；读取不到的指标注明未取得 |

开发者工具重新编译当前工程，再打开本次手机版本。保留编译结果和控制台警告原文。此前模板编译通过属于历史证据。

使用虚构或脱敏材料。在独立测试记录中做删除、失败和重试。由测试者主动把样例准备到自己的微信聊天及手机相册；助手本轮仅提供清单，未代发消息。

## 2. 固定材料与正确答案

| 材料 | 准备方式 | 放置位置 |
|---|---|---|
| A：两页发票 PDF | 两页分别可辨认，标记 A1、A2；使用虚构数据 | 第一组发票 |
| B：付款截图 PNG | 可使用 tests/fixtures/payment.png | 第一组附件，第一个 |
| C：另一张附件 JPG | 可使用 tests/fixtures/invoice.jpg；本用例按附件导入 | 第一组附件，第二个 |
| D：一张图片发票 JPG | 可复用 tests/fixtures/invoice.jpg；归属第二组 | 第二组发票 |

第一组描述输入以下两行，第二行“和”后保留两个空格：

```text
饮料 & 文具 <原文>
第二行，保留 {括号} 和  空格
```

第二组描述：`打印费用`。标题先留空，验证默认创建时间；后续再改为“真机验收 0907”。

初次输出 PDF：A1、A2、B、C、D，共五页。Word 第一组第 1—4 页，第二组第 5 页。调换两组后：D、A1、A2、B、C；Word 打印费用第 1 页，饮料与文具第 2—5 页。页码指合成 PDF 的物理页序。

电脑测试生成的 .cache/tests/acceptance.pdf 和 acceptance.docx 是预期输出参考；手机仍需从原材料重新生成。现有参考输出第一组描述与上文一致。

## 3. 一次集中完成的主流程

每项结果填写“通过／失败／未执行”，附截图、文件名或错误原文。所有空白结果默认未执行。

| 编号／对应需求 | 操作 | 通过标准 | 结果／证据 |
|---|---|---|---|
| M01／AC-01 | 首页新建，标题留空，填写第一组描述；聊天选 A，相册选 B、C | 发票与附件分区正确；材料可预览；返回后描述和材料保留；此时尚无生成结果 | 未执行 |
| M02／AC-05 | 保存并添加下一项，填写第二组描述及 D；保存返回列表 | 两组独立，第一组内容完整；默认标题使用创建时间 | 未执行 |
| M03／AC-02 | 点击生成，记录耗时与提示 | 进入结果页，共五页；打开 PDF 逐页核对 A1/A2/B/C/D，无裁切、空页或旋转错误 | 未执行 |
| M04／AC-02 | 打开 Word | 中文、符号、换行、连续空格完整；页码 1—4、5；检查真实排版、长文字换行与页边距 | 未执行 |
| M05／AC-03 | 返回列表，用手柄将第二组拖到第一组，再生成 | PDF 为 D/A1/A2/B/C；Word 为 1、2—5；描述跟随组 | 未执行 |
| M06／材料排序 | 在原第一组将附件 C 拖到 B 前，再生成 | 两组页码仍为 1、2—5；PDF 为 D/A1/A2/C/B；发票仍在附件前 | 未执行 |
| M07／AC-05 | 修改第二个显示组的描述；移除一个附件并取消一次移除确认，再确认移除；重新导入恢复 | 取消保持原状；确认只影响目标材料；另一组完整；原材料预览可用 | 未执行 |
| M08／版本一致 | 修改标题或描述后先查看上次结果，再重新生成 | 旧结果标识为上次生成；旧文件保留原文；新结果使用本次标题、描述、材料和页码 | 未执行 |
| M09／打开导出 | 分别打开 PDF、DOCX；各取消一次分享，再由测试者主动导出到自己的聊天 | 取消后仍可操作；聊天中两份文件可重新打开且内容匹配；仅弹出分享面板尚不足以判定导出成功 | 未执行 |
| M10／AC-06 | 返回首页→独立历史→打开记录→修改再生成 | 历史按创建时间组织；原材料、描述、顺序、结果可读取，编辑后生成成功 | 未执行 |
| M11／AC-06 | 确认已保存后关闭小程序，从最近使用重新进入；再做一次退出微信后重开 | 同一 AppID 下记录和原文件可恢复，两份输出可打开、分享 | 未执行 |

## 4. 空白、部分草稿与取消

| 编号／需求 | 操作 | 通过标准 | 结果／证据 |
|---|---|---|---|
| E01／AC-08 | 新建直接返回；另建仅空白组；另建仅空格换行；重开历史 | 三类完全空白记录均隐藏；其他已有记录保留 | 未执行 |
| E02／AC-09 | 分别新建仅标题、仅描述、仅发票、仅附件的四条记录 | 四条草稿均可从历史继续；标题继续选填 | 未执行 |
| E03／AC-10 | 在有内容记录添加空项；分别系统返回、保存返回；再重复点下一项三次 | 返回时空项移除；连续下一项复用空项；编号连续 | 未执行 |
| E04／AC-11 | 仅描述、仅发票、仅附件的项目分别返回再打开 | 部分项目保留；生成提示准确列出缺项 | 未执行 |
| E05／AC-11 | 空组打开来源菜单后取消；聊天选择取消；相册取消；选择中切后台再返回完成导入 | 导入中的组保留；完成后材料挂到原组；取消并返回后空组清理 | 未执行 |
| E06／AC-09 | 在独立测试记录成功生成后清空标题、删除所有费用组，再打开历史及上次结果 | 有成功结果的记录继续保留，旧 PDF／Word 可打开 | 未执行 |
| E07／AC-04 | 无项目生成；只描述生成；只发票生成 | 准确指出缺项目、发票或描述；草稿和其他记录完整 | 未执行 |
| E08／AC-10 | 如有旧版留下的空组，升级当前版本后打开该记录 | 空组清理，其他组顺序和材料保留；缺旧版样本时标记未执行 | 未执行 |

## 5. 失败保护与使用边界

| 编号 | 操作 | 通过标准／记录要求 | 结果／证据 |
|---|---|---|---|
| F01 | 独立记录先成功生成，再加入损坏 PDF，生成失败后替换为有效 PDF 重试 | 错误定位到材料；草稿、原材料、旧输出可用；重试成功 | 未执行 |
| F02 | 分别加入加密 PDF、无页面 PDF、当前不支持的图片格式 | 保存与生成结果分开记录；失败有原因，旧结果保留；使用真实格式文件，扩展名伪装单独记录 | 未执行 |
| F03 | 一次选择有效及不支持材料，记录各文件结果 | 已成功导入部分保留；失败项清楚；可补选；重复点击期间无重复导入 | 未执行 |
| F04 | 生成中重复点击、切后台、返回；再次进入记录 | 状态可恢复，最终只显示完整配对的输出；失败后可重试 | 未执行 |
| F05 | 新用户首次选择：同意、拒绝隐私授权；系统相册权限若出现则分别拒绝和恢复 | 同意后可选择；拒绝后草稿保留且可继续其他操作；恢复授权后可重试；记录实际弹窗及返回错误 | 未执行，正式 AppID 必测 |
| F06 | 单独测试长图、横图、带旋转信息的照片、长中文文件名、多行长描述；首页左右手分别自然握持并连续点按新建入口五次 | 预览与输出完整；长截图可读性单独判定；键盘、按钮和提示可见；记录手机型号、常用握姿、是否调整握姿或误点历史，依实测调整入口位置 | 未执行 |
| F07 | 6 个以上组、6 个以上附件，拖到列表边缘及取消拖动 | 滚动和排序正确，无误点、跳项；重新打开和生成顺序一致 | 未执行 |
| F08 | 以 5／20／50 页逐级增加虚构材料；每级连续生成三次 | 每级记录文件数、字节、耗时、输出大小、卡顿及失败；这些档位是测试输入，支持上限以实测确定 | 未执行 |
| F09 | 隔离测试环境模拟写入／改名失败或配额不足；真实低空间条件另测 | 失败后旧清单、原文件、旧输出保持可用；解除条件后重试；电脑已有模拟证据，手机缺条件仍记未执行 | 未执行 |
| F10 | 已导入并保存后断网，重开记录生成和打开；恢复网络后分享 | 记录本机生成、打开与联网分享各自结果；保留失败原文 | 未执行 |

存储失败测试使用可控测试环境。避免填满日常手机或清理含真实材料的微信数据；先导出重要记录。当前代码保留旧文件，反复生成后的空间增长需要记录。清理微信数据、换设备、切换 AppID 的数据连续性分别验证，现有方案只保证保留数据的同设备同 AppID 场景。

## 6. 汇总、问题处理与阶段通过条件

```text
本轮设备／版本：
通过编号：
失败编号：
未执行编号与缺少条件：
问题：编号 + 操作步骤 + 材料格式/大小 + 预期 + 实际提示
证据：截图/输出文件/日志位置（使用脱敏内容）
耗时与容量：
修复后复测：原失败步骤 + M01—M11 主流程
```

丢记录、错误页码、内容遗漏、生成后无法打开、核心导入失败，优先处理并阻止进入体验版。可读性、拖拽及恢复问题按影响记录，修复后复测相关完整操作。

阶段一完成需要 M01—M11 与 E01—E07 的 Android 证据，以及 F01、F04 的失败恢复证据。其余项保留明确状态；正式 AppID 体验版前补 F05，公开发布前补齐容量、存储失败和支持格式边界，并在 iPhone 重走核心流程。清单编写完成只代表验收准备完成。

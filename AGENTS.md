# 本项目协作说明

## 目标与需求依据

本项目是微信报销材料整理小程序。首版在手机本地处理图片与 PDF，生成凭证 PDF 和带页码说明的 Word，并保存本机历史记录。

以用户当前明确要求和 `docs/requirements.md` 中的已确认需求为依据。技术设计中的候选方案、待验证问题和实际通过结果分别记录。

## 新对话读取顺序

1. 本文件。
2. `README.md` 与 `docs/progress.md`，确认当前能运行到哪里。
3. `docs/requirements.md` 与本次任务相关的验收案例。
4. `docs/technical-design.md` 和 `docs/initialization-plan.md` 中相关部分。
5. 相关代码、配置和实际依赖锁文件。

核对文件当前状态后再继续。文档中的“待验证”保持为待验证，直到具有对应证据。

## 开发规则

- 每个技术环节编码前先上网搜索已有代码，比较来源、功能覆盖、运行环境、许可证、依赖、维护情况和成本，记录采用或排除理由。
- 技术选择由开发助手提出明确结论和依据；影响用户体验、业务规则或费用的变化交给用户判断。
- 每次任务写明目标、涉及模块、输入输出和验收方式。修改后检查相关完整操作，同步更新进度与受影响文档。
- 页面通过共同数据约定和仓库入口操作记录；PDF 与 Word 使用同一次生成的内容和页码表。
- 导入阶段保存原文件；用户点击生成时进行图片转换、PDF 合并和 Word 生成。
- 沿用用户确认的当前执行范围。范围变化时先说明具体影响；避免重复请求已经获得的授权。
- 保留原材料与可用记录。真实报销材料、个人本地配置和凭证按各自用途存放。
- 小程序 AppID 使用当前项目的真实配置。AppSecret 等凭证应留在受保护的账号管理范围。

## 验证与汇报

- 基础检查命令为 `npm.cmd run typecheck`。
- 分别说明静态检查、微信开发者工具编译和 Android 真机验证的结果。
- 每次结束更新 `docs/progress.md`：完成内容、命令与结果、尚缺条件、下一项具体工作。
- 需求变化时同步检查需求文档、技术约定、调用位置和验收案例。
- 使用中文，先说用户能看到什么，再说明修改位置、数据流程、理由和验证证据。
